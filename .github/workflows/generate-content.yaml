name: Trigger Course Content Action

on:
  workflow_dispatch:  # Manually trigger the workflow

jobs:
  python-black:
    name: Python Black
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - name: Python Black
        uses: infoyouth/course-content-action/action.yml@main
        with:
          path: './'
