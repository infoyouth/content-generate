name: Trigger Course Content Action

on:
  workflow_dispatch:  # Manually trigger the workflow

jobs:
  call-course-content-action:
    uses: infoyouth/course-content-action/.github/workflows/course_content_action.yml@main  # Correctly reference the reusable workflow
    with:
      github_token: ${{ secrets.GITHUB_TOKEN }}  # Pass the GitHub token to the reusable workflow
